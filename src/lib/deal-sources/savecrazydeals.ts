import { createShopifySource } from './_base';

export default createShopifySource({
    id: 'savecrazydeals',
    displayName: 'SaveCrazyDeals',
    baseUrl: 'https://savecrazydeals.com',
    limit: 30,
});
